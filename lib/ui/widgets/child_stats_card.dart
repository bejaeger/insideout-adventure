import 'package:flutter/material.dart';

class ChildStatsCard extends StatelessWidget {
  const ChildStatsCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
